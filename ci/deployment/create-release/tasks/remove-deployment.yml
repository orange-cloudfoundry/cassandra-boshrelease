---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: concourse/bosh-cli
    tag: latest

inputs:
  - name: bosh-director-config
  - name: cassandra-bosh-release
  - name: remove-deploy

run:
  path: cassandra-bosh-release/ci/deployment/create-release/scripts/remove-deployment.sh
