#!/usr/bin/env bash

set -e # exit immediately if a simple command exits with a non-zero status
set -u # report the usage of uninitialized variables

# Detect # of CPUs so make jobs can be parallelized
CPUS=$(grep -c ^processor /proc/cpuinfo)
 # Available variables
# $BOSH_COMPILE_TARGET - where this package & spec'd source files are available
# $BOSH_INSTALL_TARGET - where you copy/install files to be included in package

# Source script supplied by java vendoring package
source /var/vcap/packages/openjdk-8/bosh/compile.env

# Compile broker and move it (direct access to cassandra-open-service-broker directory defined in spec file)
cd cassandra-open-service-broker
./mvnw clean install -DskipTests=true
mv target/cassandra-open-service-broker-0.0.1-SNAPSHOT.jar ${BOSH_INSTALL_TARGET}/cassandra-broker.jar

# Remove broker source files
cd ${BOSH_COMPILE_TARGET}
rm -rf cassandra-open-service-broker

# Move produced jar file from COMPILE_TARGET to INSTALL_TARGET
cp -r ${BOSH_COMPILE_TARGET}/cassandra-open-service-broker-0.0.1-SNAPSHOT.jar  ${BOSH_INSTALL_TARGET}/cassandra-broker.jar

